/// Stores all the messages used throughout the app that communicates
/// with the user of the package.

const String kCommandRunnerDescription =
    'The command line tool that performs the session replay actions.';

const String kCommandRunnerName = 'session_mate';

const String kCommandRunnerVersionHelp = 'Print the current version.';

const String kCommandRunnerEnableAnalyticsHelp = 'Enable Analytics.';

const String kCommandRunnerDisableAnalyticsHelp = 'Disable Analytics.';

const String kCommandDriveDescription = 'Drives guest app.';

const String kCommandDriveName = 'drive';

const String kCommandDriveSweetCoreStarts = 'Starting SweetCore...';

const String kCommandDriveSweetCoreInitialised = 'SweetCore initialised';

const String kCommandDriveLocalModeOnly =
    'Session Mate restricted to local mode only. To enable extra features 🔥 please set your API key.';

const String kCommandDriveHelpDelay =
    'Sets the delay in milliseconds between each step.';

const String kCommandDriveHelpLogSweetCoreEvents =
    'Enable the log of SweetCore events.';

const String kCommandDriveHelpVerbose =
    'Noisy logging, including all shell commands executed.';

const String kCommandDriveHelpPath = 'Sets the path of the guest application.';
const String kCommandDriveHelpApiKey =
    'The API key provided when you bought your subscription. Contact dane@sessionmate.dev if you didn\'t receive one when you signed up.';

const String kCommandDriveHelpAdditionalCommands =
    'The command to attatch at the end of flutter run command. Commonly used for passing in dart-defines or flavors.';

const String kCommandUpdateDescription =
    'Updates session_mate_cli to latest version.';

const String kCommandUpdateName = 'update';
